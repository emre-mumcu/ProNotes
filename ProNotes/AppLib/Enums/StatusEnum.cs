namespace ProNotes.AppLib.Enums
{
    // Status is used to describe an outcome of an operation (e.g. success/fail).
    // Status is a final (resulting) state.
    public enum StatusEnum
    {
        Passive = 0,
        Active = 1
    }
}